<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:color="@color/yTab" android:state_selected="false"/> <!-- un selected -->
    <item android:color="@color/yMain" android:state_selected="true"/> <!-- selected -->
    <item android:color="@color/yTab"/> <!-- default 这个要在最底下，不然没有效果-->
</selector>
