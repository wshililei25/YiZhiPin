<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:color="@color/yCategory" android:state_selected="false"/> <!-- un selected -->
    <item android:color="@color/yMain" android:state_selected="true"/> <!-- selected -->
    <item android:color="@color/yCategory"/> <!-- default 这个要在最底下，不然没有效果-->
</selector>
